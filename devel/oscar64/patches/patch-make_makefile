Index: make/makefile
--- make/makefile.orig
+++ make/makefile
@@ -5,25 +5,30 @@ objects = $(patsubst ../oscar64/%.cpp,%.o,$(sources))
 
 CXX = c++
 CPPFLAGS = -g -O2 -std=c++11 -Wno-switch
- 
- $(shell mkdir -p ../bin)
- 
+
+$(shell mkdir -p ../bin)
+
 ifdef WINDIR
 	linklibs = -lpthread
 else
 	UNAME_S := $(shell uname -s)
-	
+
 	ifeq ($(UNAME_S), Darwin)
 
 		linklibs = -lpthread
 	else
-		# MSVC
-		linklibs = -lrt -lpthread
-		# MinGW
-		#linklibs = -lversion -lpthread
+		ifeq ($(UNAME_S), OpenBSD)
+			linklibs = -lpthread
+			CPPFLAGS += -DDATAPATH=\"$(DATAPATH)\"
+		else
+			# MSVC
+			linklibs = -lrt -lpthread
+			# MinGW
+			#linklibs = -lversion -lpthread
+		endif
 	endif
 endif
- 
+
 %.o: ../oscar64/%.cpp
 	$(CXX) -c $(CPPFLAGS) $< -o $@
 
