$OpenBSD$
--- fop.orig	Fri Apr  3 22:06:16 2015
+++ fop	Fri Apr  3 22:09:24 2015
@@ -86,6 +86,8 @@ case "`uname`" in
            ;;
 esac
 
+FOP_HOME=/usr/local/share/fop
+
 if [ -z "$FOP_HOME" -o ! -d "$FOP_HOME" ] ; then
   ## resolve links - $0 may be a link to fop's home
   PRG="$0"
