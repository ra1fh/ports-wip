#!/bin/sh

daemon="/usr/local/bin/node_exporter"
daemon_flags="-collectors.enabled='filesystem,loadavg,netdev,time'"
daemon_user=_prometheus

. /etc/rc.d/rc.subr

pexp="${daemon}.*"
rc_bg=YES
rc_reload=NO

rc_cmd $1
